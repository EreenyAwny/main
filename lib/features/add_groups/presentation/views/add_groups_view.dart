import 'package:flutter/material.dart';

class AddGroupds extends StatelessWidget {
  const AddGroupds({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Column(
        children: [Text("data")],
      ),
    );
  }
}
